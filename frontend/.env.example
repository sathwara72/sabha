# Origin of the Laravel backend API (no /api suffix, no trailing slash).
# Local development:
NEXT_PUBLIC_API_ORIGIN=http://localhost:8000
# Production example (set this in Vercel → Project → Settings → Environment Variables):
# NEXT_PUBLIC_API_ORIGIN=https://api.yourdomain.com
